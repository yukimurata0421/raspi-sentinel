name: Publish PyPI

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      repository:
        description: "Target package index"
        required: true
        default: "testpypi"
        type: choice
        options:
          - testpypi
          - pypi
      target_ref:
        description: "Git ref to package (for example: v0.9.0)"
        required: false
        default: "main"
        type: string

permissions:
  contents: read
  id-token: write

jobs:
  publish-testpypi:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.repository == 'testpypi'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ github.event.inputs.target_ref }}
      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"
      - name: Show expected Trusted Publisher claims (TestPyPI)
        run: |
          echo "repository=${GITHUB_REPOSITORY}"
          echo "workflow_ref=${GITHUB_WORKFLOW_REF}"
          echo "ref=${GITHUB_REF}"
      - name: Build package
        run: |
          python -m pip install --upgrade pip
          python -m pip install build twine
          python -m build --sdist --wheel
          twine check dist/*
      - name: Publish to TestPyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          repository-url: https://test.pypi.org/legacy/

  publish-pypi:
    if: github.event_name == 'release' || (github.event_name == 'workflow_dispatch' && github.event.inputs.repository == 'pypi')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ github.event_name == 'release' && github.ref_name || github.event.inputs.target_ref }}
      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"
      - name: Show expected Trusted Publisher claims (PyPI)
        run: |
          echo "repository=${GITHUB_REPOSITORY}"
          echo "workflow_ref=${GITHUB_WORKFLOW_REF}"
          echo "ref=${GITHUB_REF}"
      - name: Build package
        run: |
          python -m pip install --upgrade pip
          python -m pip install build twine
          python -m build --sdist --wheel
          twine check dist/*
      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
