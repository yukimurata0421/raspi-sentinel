# Scenario Tests

Place newly added cross-module behavior tests here.

- focus: policy transitions and multi-check interactions
- allows richer fixtures than unit tests
- prefer deterministic, state-transition-oriented assertions
