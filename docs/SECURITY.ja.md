# セキュリティポリシー

## サポート対象

セキュリティ修正は最新のリリースライン（現時点では `v0.8.x`）を対象に実施します。  
古いタグは原則バックポート対象外です。

## 脆弱性報告

公開前に、必ず非公開チャネルで報告してください。

推奨窓口:

- GitHub Security Advisories（非公開報告）
- またはメンテナ連絡チャネル

報告時に含める内容:

- 影響バージョン / タグ
- 再現手順
- 影響範囲
- 秘匿情報を除いたログや証跡

## 開示と修正の流れ

1. 受領・再現確認
2. 修正と回帰テスト追加
3. 修正版リリースとリリースノート公開
4. 修正版公開後に詳細開示

## 運用上の注意

- `/etc/raspi-sentinel/config.toml` は `root:root` + `0600` を推奨
- command 文字列に長期シークレットを直書きしない
- webhook URL / token をログや events に露出させない
- 本番で timer を有効化する前に `validate-config --strict` を実行

English policy: [../SECURITY.md](../SECURITY.md)
