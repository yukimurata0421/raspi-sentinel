# Release Notes Index

`docs/release-notes/` stores release-body markdown used by tag-based release workflow.

Historical note:

- `v0.3.0` is referenced in versioning history as the first formally aligned release line,
  but a standalone release-notes markdown file was not preserved for that tag.
- release-note files begin at `v0.3.1`.
